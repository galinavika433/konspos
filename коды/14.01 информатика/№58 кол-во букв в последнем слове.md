```typescript

function lengthOfLastWord(s: string): number {
    s = s.trimEnd(); // Убираем пробелы справа
    let length = 0;
    for (let i = s.length - 1; i >= 0; i--) {
        if (s[i] === ' ') break; // Столкнулись с пробелом - конец слова
        length++;
    }
    return length;
}

```