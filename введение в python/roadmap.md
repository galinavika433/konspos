- Python = сверху вниз
- `:` + отступ = блок
- переменные = данные
- 4 типа: int, float, str, bool
- условия = if/elif/else
- циклы = for/while
- функции = def + return
- список = [], изменяется
- tuple = (), нельзя менять
- set = уникальные
- dict = ключ: значение
- try/except = защита от ошибок

**основу всю посмотрела**