инструкция - это отдельная команда

задаем переменную х, если х больше 3 то все ок, т е правильно
```python
x = 5
print(x)

if x > 3:
    print("ok")
```
каждая строка, отдельная инструкция


много инструкций подряд:
задаем имя, возраст (вообще все что угодно), выводим и то и другое
```python
name = "Anna"
age = 20

print(name)
print(age)
```