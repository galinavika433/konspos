операторы

`+ - * /` математика
`//` без остатка
`%` остаток
`==` сравнение
`and/or/not` логика

## 1. арифметика

```
+ - * / // % **
```

- `//`  целая часть
- `%`  остаток
- `**`  степень

## 2. сравнение

```
== != > < >= <=
```

## 3. логика

```
and or not
```
